require('dotenv').config(); const { Client } = require('pg'); async function fixDb() { const client = new Client({ connectionString: process.env.DATABASE_URL }); try { await client.connect(); console.log('Connected successfully. Altering orders table...'); await client.query('ALTER TABLE orders ADD COLUMN IF NOT EXISTS item_total DECIMAL(10, 2) DEFAULT 0.00, ADD COLUMN IF NOT EXISTS delivery_charge DECIMAL(10, 2) DEFAULT 0.00, ADD COLUMN IF NOT EXISTS gst_amount DECIMAL(10, 2) DEFAULT 0.00, ADD COLUMN IF NOT EXISTS platform_fee DECIMAL(10, 2) DEFAULT 0.00, ADD COLUMN IF NOT EXISTS restaurant_share DECIMAL(10, 2) DEFAULT 0.00, ADD COLUMN IF NOT EXISTS delivery_instructions TEXT, ADD COLUMN IF NOT EXISTS restaurant_instructions TEXT;'); console.log('Table altered successfully! You can now place orders.'); } catch (error) { console.error('Error altering table:', error); } finally { await client.end(); } } fixDb();
